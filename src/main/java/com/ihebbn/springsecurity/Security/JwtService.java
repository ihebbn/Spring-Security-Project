package com.ihebbn.springsecurity.Security;

import org.springframework.stereotype.Service;

@Service
public class JwtService {
    // this jwt service is the service that will generate the token, decode the token, extract the information from the token,
    // validate the token, it will do anything related to jwt token that we will receive within our request


}
